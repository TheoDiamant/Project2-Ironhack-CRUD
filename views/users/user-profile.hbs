<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <title>UserProfile: {{ userInSession.username }}</title>
  </head>
  <body>
<div class="card">
  <div class="card-header">
    <ul class="nav nav-tabs card-header-tabs" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
        <a class="nav-link active" id="home-tab" data-bs-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Best Scores</a>
      </li>
    </ul>
  </div>
  <div class="card-body">
    <div class="tab-content" id="myTabContent">
      <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
                      {{#if userInSession.score}}
                  {{#each highScoresByGame}}
                    <p style="color: white;">Your high score in <b>{{this.gameName}}: {{this.highScore}}</b></p>
                    <img src="{{this.gameLogo}}">
                  {{/each}}
                {{else}}
                  <p>No score available.</p>
                {{/if}}
              {{#if userInSession}}
      </div>
    </div>
  </div>
</div>
  </div>

<div style="display: flex; flex-direction: column; justify-content: center; align-items: center; margin-top: 1em;">
   <button  class="btn btn-secondary">
    <a href="/editinfo" style="color: white">Edit user info</a>
  </button>
   <form action="/delete" method="POST">

   <button style="font-size: 1.5em;" class="btn btn-danger" type="submit"> Delete the account </button>

</form>
{{else}}
  
  <br><p>Still no logged in user, sorry!</p>
 
{{/if}}

</div>

<script>
  window.onload =() => {
        // Assign click event listeners to the tabs
        var triggerTabList = [].slice.call(document.querySelectorAll('#myTab a'))
        triggerTabList.forEach(function (triggerEl) {
            var tabTrigger = new bootstrap.Tab(triggerEl)
            triggerEl.addEventListener('click', function (event) {
                event.preventDefault()
                tabTrigger.show()
            })
        })
  }
  </script>

  </body>

  
